info:
  name: Get Organisation
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/organisations/9a592742-3bbb-4023-beaf-a37827d0efc1"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
